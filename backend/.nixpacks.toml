# .nixpacks.toml for Python FastAPI backend
[phases.setup]
  nixPkgs = ["python39", "pip"]

[phases.build]
  dependsOn = ["setup"]
  cmds = [
    "pip install -r requirements.txt"
  ]

[phases.start]
  dependsOn = ["build"]
  cmd = "python start_server.py"